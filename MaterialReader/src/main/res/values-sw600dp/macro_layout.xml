<resources>

    <!-- book title font size -->
    <dimen name="detail_title_text_size">42sp</dimen>

    <!-- general margins -->
    <dimen name="toolbar_horz_margin">16dp</dimen>
    <dimen name="fab_margin">16dp</dimen>

    <!-- book title spacing between subtitle -->
    <dimen name="detail_title_bottom_spacing">8dp</dimen>

    <!-- book body text line spacing -->
    <item name="detail_body_line_spacing_multiplier" format="float" type="fraction">1.3</item>

    <!-- text horizontal margin inside card -->
    <dimen name="detail_text_horiz_margin">64dp</dimen>

    <!-- text top padding inside card -->
    <dimen name="detail_text_top_padding">32dp</dimen>

    <!-- backdrop image height -->
    <dimen name="detail_image_height">500dp</dimen>

    <!-- display list numbers in small tablets -->
    <integer name="list_number_visibility">0</integer>

</resources>
